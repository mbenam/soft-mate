# M8 Driver — Global Bug List

A single index of every real, hardware-confirmed bug found while building and hardening the
serial driver (`src/tools/m8/`), across all sessions. This is an index, not a duplicate — each
entry links to the fuller writeup in `M8_DEVICE_CONTROL_SPEC.md` where the hardware evidence,
symptom, and exact fix live. Status values: **FIXED** (in the codebase now), **REVERTED** (a fix
was built, hardware-confirmed unreliable, and deliberately removed rather than shipped),
**DATA** (not a logic bug — a field map had wrong/nonexistent coordinates, corrected), **OPEN**
(found, not fixed).

Numbering is chronological across sessions, not by severity.

---

## Driver bugs (in scope)

| # | Component | Status | Summary |
|---|---|---|---|
| 1 | `ScreenModel.h` — `findScreenForField` | FIXED | Type-blind for the Instrument screen: could only resolve field names against the Sampler map, so MacroSynth-only fields (SHAPE/TIMBRE/COLOR/REDUX) resolved to `Screen::UNKNOWN` before `moveCursorTo` ever got a chance to use the type-aware lookup. |
| 2 | `ScreenModel.h` — `identifyScreen` | FIXED | `stripDigits` only stripped trailing *decimal* digits, but phrase/chain/table IDs are hex (00-FF) and can end in a letter — `"PHRASE FC"` never matched `"PHRASE"`, so `gotoScreen` returned `UNKNOWN` for roughly half of all possible IDs. |
| 3 | `Primitives.cpp` — `moveCursorToGrid` | FIXED | Grid-screen cursor detection trusted a static, non-cursor `<` glyph over the real accent-cyan cursor color on PHRASE/TABLE, and a naive color scan (no row floor) could pick up the column-header label instead of the real cursor. |
| 4 | `Primitives.cpp` — `editValue` (numeric branch) | FIXED | Compared the raw target string (`"0x40"`) against on-screen text that only ever shows `"40"` — never matched even on genuine convergence. Fixed to parse both sides as hex and compare numerically. |
| 5 | `M8Device.cpp` — `ScreenGrid::cursorRowY()` | FIXED | The M8 skips redrawing a row's trailing blank cells when the cursor leaves it (a cyan space looks identical to a normal space), leaving stale cyan padding at the *vacated* row. The naive topmost-cyan-cell scan picked the ghost row over the real cursor's row. Fixed by requiring the picked cell be non-space. |
| 6 | `M8Device.cpp` — `M8Device::cursorField()` X-lookup | FIXED | Sibling bug to #5, not caught by the same fix: moving between two fields *on the same row* (e.g. EFFECTS' "MOD TYPE" → "INPUT EQ") left ghost cyan blanks at the vacated field's leading columns, and the X-position lookup picked the leftmost ghost instead of the real field. |
| 7 | `Primitives.cpp` — `moveCursorTo` (row-match branch) | FIXED | Always pressed RIGHT when on the correct row but wrong field. Real M8 multi-column rows (e.g. Sampler's SLICE/AMP pair) aren't reached column-by-column via UP/DOWN — DOWN from a full-width row (SAMPLE) lands directly on the row's *right*-column field (AMP), skipping the left-column field (SLICE) entirely. Fixed to compare target column vs. current column and press LEFT or RIGHT accordingly. |
| 8 | `ScreenModel.h` — `findScreenForField` | FIXED | Resolved short field names to the wrong screen via fuzzy substring matching — `"MST_CHO"` (Mixer) substring-matches `"CHO"` (Instrument/Sampler's chorus send), and since INSTRUMENT is checked before MIXER in the screen table, `"MST_CHO"` resolved to INSTRUMENT. Fixed to require an exact match across every screen first, falling back to fuzzy matching only if none exists. |
| 9 | `ScreenModel.h` — `kEffectsFields` | DATA | Every row was off by exactly one, with the wrong column (0 instead of 8). Root cause: the map was missing the CHORUS section's first row ("MOD TYPE"), so every field inherited a +1 offset — which "worked" by accident until the offset crossed the blank gap between the CHORUS and DELAY sections, at which point `DEL_EQ` became permanently unreachable. Re-measured all 14 rows/columns from pixel data. |
| 10 | `Primitives.cpp` — `moveCursorTo` (stuck-axis fallback) | FIXED | The row/column axis-fallback (added alongside #7) misfired on 1-pixel column measurement noise (e.g. `SYSTEM`'s label measures at col 1 even though its map entry says col 0), sending it into a useless LEFT/RIGHT loop instead of just continuing to press DOWN. Fixed with a >1-column tolerance. |
| 11 | `ScreenModel.h` — `kScaleFields` | DATA | `LOAD`/`SAVE` columns were wrong (0 and 5 instead of the real 7 and 12). |
| 12 | `Primitives.cpp` — `moveCursorTo` (stuck-axis fallback, latching) | FIXED | The axis-fallback from #10 was latched — once triggered it kept pressing LEFT/RIGHT forever, which could walk off the edge of a row's own sub-fields (SCALE's per-note EN/OFFSET columns) and then loop forever once that axis also stopped making progress, without ever giving the vertical direction another chance. Fixed to alternate (vertical, horizontal, vertical, ...) rather than latch. |
| 13 | `Primitives.cpp` — `readCursorValue` | FIXED | Never actually stripped the field's label from `cursorMainText()` despite its own doc comment claiming it did. On multi-column rows (e.g. `"AMP" + "FF"` → `"AMPFF"`), `editValue`'s numeric convergence check parsed the *leading* hex run — since `'A'` is a valid hex digit as well as a letter, it silently read "10" instead of "255" and could never match any real target, running all 256 steps and clamping at `0xFF`. Fixed by looking up the field's exact label from the field maps and stripping precisely that (a generic "skip leading letters" heuristic fails too — hex letters A-F are also alphabetic, e.g. `"TIMBRE"` ends in `E`). |
| 14 | `Primitives.cpp` — `editValue` (numeric stepping direction) | FIXED | The stepping loop only ever pressed `value_inc`, never `value_dec`. Values clamp at `0xFF` rather than wrapping, so a target below the current value was structurally unreachable. Fixed to pick direction from the parsed current-vs-target comparison each iteration. |
| 15 | `Primitives.cpp` — `loadFile` (climb to PROJECT) | FIXED | Used a separate, ad-hoc "press SHIFT+UP up to 8 times" loop instead of the already-hardened `gotoScreen`, duplicating a working mechanism with a less reliable one. SHIFT+UP from INSTRUMENT could land on INST_POOL instead of climbing toward PROJECT, with no way to recover (`rc=10`). Fixed by calling `gotoScreen(dev, Screen::PROJECT, ...)` directly. |
| 16 | `Primitives.cpp` — `loadFile` (file-selection scroll direction) | FIXED | The file-selection loop only knew how to scroll DOWN toward a target once it had scrolled out of the decoded viewport — a target *above* the current cursor position in the LOAD PROJECT browser (e.g. the first file in the list, browser left scrolled near the bottom from a prior manual browse) could never be reached (`rc=14`). Fixed by scrolling to the top of the list before searching. |
| 17 | `ScreenModel.h` — `kProjectFields` (`SAMPLEROOT`) | DATA | Field never existed on this firmware. Exhaustive search (SYSTEM SETTINGS submenu, MIDI settings submenu, LOAD PROJECT browser's OPT-key behavior) found no reachable field matching it anywhere; DOWN from `SYSTEM` (the real last row) never advances or scrolls, tested with both a tap and a 400ms hold. Removed the phantom field entry. Notably, this project's own SDL3 UI clone models `SAMPLE_ROOT` as a normal reachable field — a clone-vs-hardware discrepancy, flagged separately, out of driver scope. |
| 18 | `Primitives.cpp` — MIXER chain-walk (`isMixerChainField`) | FIXED, then REVERTED with the feature | Included `"IN_VOL"` in the recognized set, so the chain walker's own call to reach its entry point (`moveCursorTo(dev, "IN_VOL", ...)`) redirected straight back into the chain walker — infinite recursion. A single `cursor MIX_VOL` call hung 30+ seconds before being killed. |
| 19 | `Primitives.cpp` — MIXER chain-walk (hop retry) | FIXED, then REVERTED with the feature | On a hop mismatch, retried by pressing the *same key again* rather than just waiting longer. If the first press had actually succeeded but the read caught a stale frame, the second press advanced a second logical step — directly reproduced landing 2 fields past the intended target after 3 "failed" attempts that had each actually moved the cursor. |
| 20 | MIXER compound widget navigation (`MST_CHO`/`MST_DEL`/`MST_REV`/`MIX_VOL`/`LIM_VAL`/`DJF_FREQ`/`DJF_RES`) | **OPEN — but the central evidence did not reproduce; see the 2026-08-14 note at the end of this entry** | Root-caused, not fixable with a fixed key sequence: this widget's navigation is not a pure function of (current field, key pressed) — it depends on hidden state (most likely a per-column-group "last selected index" the M8 remembers independently of the visible cursor). Proven directly: a fully-isolated, hop-by-hop-verified path (`IN_VOL --RIGHT--> USB_VOL --RIGHT--> MIX_VOL --RIGHT-->...`) reproduced a *different* result on identical re-test after bugs #18/#19 were fixed. A fixed-sequence implementation was built, then deliberately reverted rather than shipped unpredictable. Also confirmed: the M8 remembers per-screen cursor position across screen re-entry, so a "bounce out and back" reset does not help escape this widget — only a bounded run of plain UP presses reliably does. `DJF_TYP`'s on-device position could not be located at all. **RE-TEST 2026-08-14 (fw 6.5.2, COM3, `m8drv`):** the non-reproducibility claim **did not reproduce**. Two runs, each `goto MIXER` → `home` (so both start from `OUTPUT VOL`, controlling for the M8 remembering per-screen cursor position — the earlier re-test did not control for this) → `probe RIGHT --times 6`, produced **byte-identical** cursor sequences: `E0`(row 120) → `EQ`(160) → `MIX E0`(170) → `LIM 00`(180) → `DJF 80`(190) → no further movement. That path traverses **three of the eight fenced fields** (`MIX_VOL`, `LIM_VAL`, `DJF_FREQ`) deterministically. The sixth press not moving is RIGHT exhausted at the right edge, not unpredictability — `OTT` (`DJF_RES`) sits below `DJF` and needs DOWN. This matters because the original finding rested on hop-by-hop cursor *reads*, and three defects in exactly that were fixed the same day (#22/#23/#24); #24 in particular made positions go stale **within** a connection while reading correctly in any fresh process, which is precisely what "identical re-test, different result" looks like. **Still OPEN, but narrowed from eight fields to three, and re-characterised.** `cursor MIX_VOL` from a homed start now **succeeds** (landed on `MIX E0`, row 170), so it is unfenced. `cursor MST_CHO` still fails (`rc=7`, 40 steps exhausted). **Leading hypothesis (2026-08-14): the map points at the wrong MX/DE/RE.** `{"MST_CHO", "MX", 10, 16}` resolves to y=190 col 10 under this file's `y = (row+3)*10` convention, and `MX` text *is* there — but there are **two** MX/DE/RE groups on this screen. y=170 carries three values (`E0 E0 FF`) with `MX DE RE` **labels beneath them at y=180**, which is the master-send block; y=190–210 is a separate 3×2 block of `MX`/`DE`/`RE` × `INPUT`/`USB` send levels. The map appears to aim at the input-send block rather than the master sends, which would make this the same DATA class as #9/#11/#17 and straightforwardly fixable by re-measuring. An earlier revision of this note asserted the map was correct; that checked the coordinate arithmetic but not *which* `MX` it landed on. Traversal data, both from `OUTPUT VOL` after `home`: `probe RIGHT` walks rows 50 → 120 → 160(`EQ`) → 170(`MIX E0`) → 180(`LIM 00`) → 190(`DJF 80`); `probe DOWN` walks 50 → 120 → 170(`00`) → 180(`INPUT`) → stop. The two agree on rows 170/180 but report *different fields* there, so they traverse different columns — which cannot be resolved from field names alone. With `cursor_col` added, the traversal resolves: RIGHT walks (row,col) 120,176 → 160,224 → 170,192 → 180,192 → 190,192; DOWN walks 120,104 → 170,104 → 180,104. **Neither ever reaches col ≤ 64 (cells 0–8) on row 170**, which is where the master-send values `E0 E0 FF` sit. So those three are not reachable from `OUTPUT VOL` by RIGHT or DOWN; LEFT or UP from elsewhere would have to be mapped. **CLOSED AS WON'T-FIX for now (2026-08-14), deliberately, not by oversight:** this is three fields on one screen, the neighbouring input/USB mixer is documented in `status.md` as never going to be implemented, and `MST_CHO`/`MST_DEL`/`MST_REV` are editable in the clone's own UI — driving them *on hardware* buys only parity-rig convenience. The fence keeps them from wasting an agent's time, `--unfence` exists for anyone who wants to try, and the traversal data above means the next attempt starts from measurements rather than from scratch. Reopen if on-device authoring of master sends ever actually matters. |
| 21 | Instrument screen TYPE field — unmapped LOAD/SAVE column | **OPEN** | TYPE's row also has an unmapped LOAD/SAVE button pair at a higher column, with no field-map entry to disambiguate. If the cursor happens to already be parked on the LOAD button when `cursor TYPE` is called, `identifyCursorField`'s "col ≤ gridCol, greatest wins" rule can report "already on target" without verifying the actual column, leaving `editValue`'s value-cycling presses operating on the wrong widget. Found while setting up a MacroSynth-type test; did not affect any counted acceptance runs (which all navigated to TYPE fresh). Not fixed. **Confirmed live and re-root-caused on hardware 2026-08-24 (fw 6.5.2, FMSYNTH instrument). The cause stated above is stale.** LOAD and SAVE *are* mapped now -- cols 22 and 27, in all five instrument-type maps, added by #31's fix -- so "no field-map entry to disambiguate" is no longer true, and the entry read as fixed-by-side-effect. It is not. Reproduction: `cursor TYPE`, then one `RIGHT` onto LOAD, then `cursor TYPE` again -- it reports success and the cursor does not move. With the cursor parked on SAVE, `m8_nav --dump-screen` says `cursor_field: " TYPE    SAVE"`, `cursor_text: "TYPE"`. The driver names TYPE while sitting on SAVE. **Actual mechanism:** `identifyCursorField` derives its column from `dev.cursorField()`, which returns the *leftmost* accent cell on the row. On this row the label "TYPE" is accented as well as the selected widget, so the leftmost accent is always the label -- `gridCol` comes back as 1 whether the cursor is on TYPE's value, on LOAD, or on SAVE. The mapped columns 22 and 27 are therefore unreachable by that lookup and the `col <= gridCol, greatest wins` rule can only ever answer TYPE. Same family as #6, where the X lookup picked a leftmost ghost cell. **Why it is dangerous:** the cursor can sit on SAVE while the driver believes it is on TYPE, and SAVE overwrites the instrument file. **Not fixed, deliberately.** The fix has to use the accent's extent or its text rather than its start, and `identifyCursorField` backs every `cursor` and `set` call on every screen -- a wrong change there breaks navigation silently and everywhere, which is the failure class this list is full of. It wants a session that can verify per-screen on hardware, not a one-line patch. |

| 22 | `M8Device.cpp` — `ScreenGrid::cursorRowY()` | FIXED | Reported the **column-header row** as the cursor row on grid screens, so `cursor_row`/`cursor_field` never advance there and `moveCursorToGrid` cannot converge. This is the second half of #3 ("a naive color scan (no row floor) could pick up the column-header label instead of the real cursor"), which was fixed in `moveCursorToGrid` but never in `cursorRowY()` itself. Evidence (2026-08-14, fw 6.5.2, COM3, via `m8drv probe`/`goto`): on SONG, `cursor_field` reports `"1"` at `cursor_row: 50`, and y=50 is the **track-number header** (`1 2 3 4 5 6 7 8`) — the first data row is y=60 (`00 00 -- ...`). Three DOWN presses at `hold_ms: 40` left it pinned at 50 with `baseline_drift: 0`, and `cursor-grid 4 2` failed `rc=7`. PROJECT is unaffected (cursor tracks 50→60→70→80 through TRANSPOSE/GROOVE/SCALE) because its cursor is accent-coloured *text*, which the scan is built for. **Root cause, measured (`m8drv inspect --key DOWN`, fw 6.5.2, COM3, 2026-08-14):** accent-foreground cells sat at `(row 5, col 19) = "6"` — the *column*-header indicator showing which track column the cursor is on — and at `(row 11, cols 1-2) = "05"` plus `(row 11, cols 19-20) = "--"`, the cursor's own row. One DOWN press moved the latter to `(row 12, cols 1-2) = "06"` and left the header indicator untouched. So the presses were landing, the cursor *was* accent-foreground and detectable all along, and the sole defect was that `cursorRowY()` took the **topmost** accent cell — which on a grid screen is always the header, because the header sits above the data and tracks the cursor too. `rect_count` was 0 and no accent-background cell existed, ruling out both the rect-fill and inverse-video theories. **Fix:** prefer an accent (or `'<'`) marker in the left label columns (`x < 24`) before falling back to the generic scan; the M8 recolours the cursor row's own leading row-number, which is unambiguous. Tests: `cursorRowY prefers the row label over the column header` and `cursorRowY still finds a form-screen cursor label` (`[hwdecode]`). The same run also confirmed bug #5's ghost mechanism live on hardware — `(row 11, col 3, ' ')` survived the press that moved the cursor — so the `ch != ' '` guard is load-bearing in both branches. **Superseded notes:** an earlier revision of this entry blamed the `ch == 0x3C` (`<`) branch (wrong — SONG displays no `<` at all), and a later one blamed a rect-fill/inverse-video cursor (wrong — neither exists on this screen). |

| 23 | `Primitives.cpp` — `moveCursorToGrid` column handling | FIXED | Every call failed `rc=7` ("could not reach step N col M") on grid screens, because the column half was wrong in three independent ways that compounded. (a) `detectColPitch()` returned the smallest gap between any two non-space cells anywhere on screen — 8px, the pitch of adjacent glyphs *inside* one cell — not the 24px pitch between SONG's track columns. (b) Column 0's X was calibrated from wherever the cursor currently was, so the current column always computed as 0. (c) `findCursorCell()` returns the **leftmost** accent cell on the cursor's row, which is the row-number label; its X does not change when the cursor moves sideways, so the column reading was pinned regardless. Net effect: with pitch 8, "col 2" meant 16px right of the cursor — *inside* the current cell, a position RIGHT (a full 24px hop) can never land on — so the loop oscillated 0→3→0→3 until `maxSteps` ran out. Confirmed on hardware after bug #22 was fixed: `probe DOWN` tracked rows 120→130→140→150 correctly while `cursor-grid 4 2` still failed, isolating the defect to the column axis alone. **Fix:** derive column edges from the column-header row (each run of consecutive non-space header cells is one column, and its start X is that column's left edge), and read the current column from the accent-highlighted header cell — the M8 highlights the header cell of the column the cursor is on, which is a direct readout rather than a measurement (confirmed fw 6.5.2: the track-6 header digit was accent-coloured while the cursor sat in track 6). Falls back to the rightmost accent cell on the cursor's row when no header cell is accented, since header highlighting is verified on SONG only. `targetCol` is now range-checked against the actual column count instead of failing opaquely. `detectColPitch()` deleted — it had no other caller. |

| 24 | `Primitives.cpp` — `findCursorCell` | FIXED | Missing the `c.ch != ' '` guard, so it latched onto the accent-coloured **blank** the M8 leaves at a vacated row and reported the OLD row indefinitely. Third site of the #5/#6 ghost family: `ScreenGrid::cursorRowY()` got the guard (#5), `M8Device::cursorField()`'s X-lookup got it (#6), and this one never did — despite being what every grid-screen position read goes through. **Why it stayed hidden until now:** `open()` sends `'R'`, a full-framebuffer resend that repaints ghosts away, so any *one-shot* `m8_nav` invocation reads correctly. The bug only appears on the second and later reads **within one connection** — i.e. it was invisible until the driver stopped spawning a process per command. Evidence (fw 6.5.2, COM3, 2026-08-14, `m8drv probe DOWN --times 3`): `cursor_row` advanced 150→160→170 across three presses while `grid_step` stayed pinned at `8`, the value from before the first press; a following `probe RIGHT` in a fresh process read `grid_step` as `11` (correct for row 170) and tracked `grid_col` 3→4→5 cleanly. This is also why `cursor-grid 7 5` failed while `cursor-grid 4 2` had succeeded — the latter ran right after an `'R'` with no ghosts yet and its start step already read 4, whereas the former needed vertical movement, whose progress the pinned read hid, so it pressed DOWN to the bottom and gave up on the no-progress counter. Test: `gridCursorPosition ignores the ghost row the cursor left` (`[hwdecode]`). |

| 25 | `Primitives.cpp` — `editValue` numeric stepping | FIXED | Single-stepped the value, up to 256 presses, each carrying a `readSettled(120, 200, 1200)`. At that ~320ms floor one byte edit costs over a minute and the worst case (0xFF→0x40) more than four, so `m8_nav --script tests/hw/set_param.m8script` presents as a hang rather than as slow work — reported 2026-08-14. Not merely an optimisation: it made `set` unusable in any interactive or scripted loop. `hw_buttons.json` had pinned `value_inc16`/`value_dec16` (EDIT+UP / EDIT+DOWN, a nibble step) since 2026-07-17, and `editValue` never used them. **Fix:** coarse-step while the remaining gap is at least one coarse step, then fine-step, which bounds a byte edit at roughly 30 iterations instead of 255. The coarse step's size is **measured at runtime**, not assumed: one probe press is issued and the observed delta recorded, because "nibble/octave increment" is a hex-shaped description with no guarantee of landing on 16 for every field type (a decimal field such as TEMPO need not behave like a hex byte), and a field where the gesture does nothing falls back to fine steps rather than stalling. Clamping at 0x00/0xFF needs no special case — the value is re-read every iteration, so a clamped step simply shows up as a smaller gap. |

| 26 | `Primitives.cpp` — `editValue` target parsing | FIXED | Parsed the **target** with `strtol(..., 0)` (bare digits = decimal) while parsing the **screen text** as base 16, so the two sides were read in different bases. `set PAN 80` therefore converged — correctly and silently — on 80 decimal = `0x50`, and the device showed `50`. Reported 2026-08-14 while setting up a stereo measurement; the earlier `set` runs all happened to be unambiguous (`0x40`/`0xC0` explicit, `02` identical in both bases), which is why it had not surfaced. Silent wrongness is the worst failure mode for an agent-driven tool: nothing in the result said anything was off. **Fix:** parse the target as base 16, matching both the device's display and the existing screen-side parse; `strtol` with base 16 still accepts an explicit `0x` prefix, so previous callers are unaffected. Does **not** regress TEMPO-style decimal fields, which were already unreachable — `readCursorValue` yields `"120.00"` whose leading hex run parses as `0x120` = 288, so no decimal target could ever match. Editing decimal fields needs a per-field base and is not attempted here. |

| 27 | `Primitives.cpp` — grid columns collapse compound cells | FIXED | `gridColumnEdges` derives one column per run of non-space **header** cells, but an FX cell is one header label (`FX1`) over **two** cursor stops: a 3-glyph command and a 2-glyph value. So the value half was unaddressable — `cursor-grid 0 3` reached `SCG`, and nothing could reach its `10`. Worse, it made `moveCursorToGrid` stall *inside* an FX cell: a press moved the cursor but not the reported column, so the closed loop counted five no-progress iterations and gave up, which is what the `MOVEGRID` timeouts on PHRASE were. Two models also disagreed in the tree — `gridDims()` said PHRASE had 9 columns and was **right**, but nothing in production used it (only a test asserting its own values), while the live `gridCursorPosition` said 6. **MEASURED 2026-08-15 (fw 6.5.2, COM3):** walking a row one press at a time, leaving PHRASE groups 3/4/5 took TWO presses each while 0/1/2 took one; on TABLE the same held for groups 2/3/4. So PHRASE has 9 stops and TABLE has 8 — `gridDims`' TABLE value of 5 was wrong for the same header-group reason. **Fix:** `gridSubStops(screen, group)` expands compound groups into their real stops, and the column readout skips the header path on those screens (one label cannot distinguish two stops) in favour of the rightmost accent cell on the data row — the same signal #23(c) established. Non-compound screens keep the verified header readout untouched. **Verified on hardware after the fix:** `grid_columns` reports 9, `MOVEGRID step=0 col=4` succeeds where it used to time out, and EDIT+UP there moved `SCG10` → `SCG20` — the value, not the command. Tests: `gridSubStops splits FX cells only`, `gridDims counts cursor stops, not header groups` (`[hwdecode]`). |
| 28 | `Semantic.h` — no transport state | FIXED | `SemanticState` carried screen, modal, live-mode, settled, cursor and grid coordinates, and **nothing for whether the device was playing**. Any probe depending on playback was therefore unverifiable, and one measurement (the SCA/SCG key numbering) was abandoned on the strength of a wrong inference because of it: `inspect` reported "the press is not landing" for a PLAY that had in fact worked. That verdict is a heuristic meaning "nothing visible changed" and cannot distinguish a press that did not land from one that landed invisibly — which is exactly this case. **MEASURED 2026-08-15 (fw 6.5.2, COM3):** the M8 marks the playing step with a `>` (0x3E) in the row-label gutter of a grid screen and removes it on stop. **No colour moves at all**, which is why every accent- and rect-based check saw nothing. **Fix:** `playheadVisible()` plus `isPlaying` / `playheadObservable` on `SemanticState`, both in the `state` JSON. The second flag matters: form screens draw no playhead, so `isPlaying` is false there regardless of the transport, and a caller must be able to tell "stopped" from "cannot tell". The gutter bound excludes the cursor's own `<` at x=0 and the chrome's `T>120`, which sits past `MAIN_X_MAX`. **Verified on hardware:** false when stopped, true when playing, and it immediately exposed that a blind PLAY press had been toggling the transport off in an earlier probe. Tests: `playheadVisible sees the gutter marker`, `playheadVisible ignores the cursor marker and the chrome` (`[hwdecode]`). |
| 29 | `ScreenModel.h` — instrument field maps, right-hand column | FIXED | **MEASURED 2026-08-18 (fw 6.5.2, COM3)**, and broader than first suspected: the right-hand column (`AMP`/`LIM`/`PAN`/`DRY`/`CHO`/`DEL`/`REV`) is at column **18** on the device, and **both** the Sampler and MacroSynth maps said 17 -- 14 entries across the two. MacroSynth additionally labelled the mixer-send row `CHO`, which does not appear on the device at all; the label is `MFX`, as the Sampler map already had it. Captured every instrument type in turn (`artifacts/inst_*.json`) and compared each map entry against the decoded grid. **Why it went unnoticed:** the column error is exactly 1, which is inside `moveCursorTo`'s >1-column tolerance from bug #10, so it never failed outright -- it degraded every right-column move to the slow path. The wrong label likewise fell through to fuzzy matching. Both are the shape of defect no hardware run reports, because nothing goes red. **Fix:** columns corrected to 18 in all five maps; MacroSynth's label corrected to `MFX` with the canonical name left as `CHO` so existing scripts still resolve. Test: `instrument maps agree on the right-hand column` (`[hwdecode]`) pins the class rather than the instances.
| 30 | `ScreenModel.h` — no field maps for WAVSYNTH / FMSYNTH / HYPERSYN | FIXED | `readInstrumentType()` has returned `"WAV"`, `"FM"` and `"HYPER"` since it was written, but `getFieldMap(Screen::INSTRUMENT, typeHint)` handled only MACROSYN and fell through to the **Sampler** map for all three. So `cursor <FIELD>` on any of them aimed at whatever the Sampler layout has at that row — silently, since the fallback is indistinguishable from a hit. This is why the entire WavSynth Phase 2/3 hardware campaign was driven by counting raw key presses (`WAVSYNTH_PHASE3_SPEC.md` §7.4 records it as a driver limitation without diagnosing it). **MEASURED 2026-08-18 (fw 6.5.2, COM3):** instrument 00 set to each type in turn and captured with `--ui-capture`; maps derived from the captures rather than from the clone's `*ScreenLayout.h`, which `docs/ui_screen_spec.md` states is deliberately not geometry-matched to the device. **Fix:** `kInstrumentWavsynthFields`, `kInstrumentFmsynthFields`, `kInstrumentHypersynFields`, wired into both `getFieldMap`'s type dispatch and `findScreenForField`'s two INSTRUMENT branches. **Known limit, recorded not fixed:** FMSynth's `RATIO`/`LEV/FB`/`MOD` and HyperSynth's `CHORD` are multi-column rows (four operators, seven note slots), and `FieldInfo` addresses a row, not a column — reaching one operator needs a column step the table cannot express. Tests: `instrument field maps exist for every synth type` (`[hwdecode]`). |
| 31 | `Primitives.cpp` — `identifyCursorField` / instrument maps | FIXED | `moveCursorTo` returned **success while sitting on the wrong field**, and the edit that followed operated on whatever was there. `identifyCursorField` picks the rightmost mapped field at or left of the cursor, with **no distance limit** -- so an unmapped cursor stop on a mapped row is handed to whatever field is furthest left on that row. `LOAD` and `SAVE` sit at columns 22 and 27 of the instrument TYPE row and were in no map, so a cursor on LOAD came back named `TYPE`. **Observed 2026-08-18 (fw 6.5.2, COM3):** `read PAN` leaves the cursor in the right column; `set TYPE MACROSYN` then walks up, lands on LOAD, is told it is on TYPE, and fails with `could not find enum MACROSYN in either direction from LOAD (field restored)`. Only the enum walk's own restore (#29's sibling fix) kept that from writing to a button. **Why it hid so long:** every earlier `set TYPE` in the toolchain approached from a left-column field, where walking up lands on TYPE directly -- the bug needs a right-column starting position, which nothing did until the right-column audit. **Fix:** `LOAD`/`SAVE` mapped on all five instrument types, and the field claim bounded to 16 columns -- wider than any real label-to-value span on these screens (widest measured: 8) and narrower than the 22 that caused this. An unmapped stop now yields *no name* rather than a wrong one, so `moveCursorTo` keeps navigating instead of declaring victory. Tests: `identifyCursorField refuses a distant field` (`[hwdecode]`).
| 32 | Device transmits a displaced framebuffer; nothing detected it | FIXED (cause found) | **The driver was not at fault, and four of my own theories about it were wrong.** Symptom: an INSTRUMENT screen whose lower rows are smeared, with words wrapping mid-token across row boundaries -- and `field not found` / `could not find enum` reported downstream, both of which read as navigation bugs. **MEASURED 2026-08-18 (fw 6.5.2, COM3)** by tapping the byte stream before SLIP decoding (`m8_nav --record-raw`, added for this): 2338 frames, every one well-formed, 890 draw-char commands all exactly 12 bytes, 40 distinct x values spanning 0-312 on an exact 8px grid. **The device sends those coordinates.** Reproducible across a **power cycle**; five captures spanning a power cycle, a forced repaint and a firmware reflash are **byte-identical** (md5 `69c2db2e...`), so it is deterministic, not a transient. **ROOT CAUSE, MEASURED 2026-08-19 (fw 6.5.2, COM3):** indexing every draw-char command by raster position shows the device paints the panel as **one contiguous raster walk**, and that walk has **exactly one discontinuity in 890 commands**. On INSTRUMENT it is at (row 6, col 9) and skips 31 cells; on INST.POOL it is at (row 6, col 4) and skips 36. Both columns are where **instrument 00's NAME value** starts on that screen, and both skips are exactly `40 - col` -- the distance to the start of the next line. The M8 breaks the line where the name should be drawn, emits nothing for it, and every character after that lands 30 cells late (30 not 31: one cell of *content* is lost as well as 31 of position). Sliding a displaced capture back 30 in raster order against a healthy one scores 496 matching characters and **zero** mismatches -- the content is intact, only its position is wrong. That is why PROJECT/MIXER/EFFECTS are clean: they draw no instrument name. **The bad byte was in device RAM, not on the card.** Opening the LOAD PROJECT browser raised `LOSE CHANGES TO CURRENT SONG?`, i.e. the working copy had diverged from disk. Reloading the same project (SCALEPROBE) from the SD card removed the fault completely: **0 jumps in 920 draw commands**, `NAME` renders inline as its 12-dash placeholder, and INST.POOL's 16 rows align. The card's file is clean -- `artifacts/SCALEPROBE.m8s` has every instrument at `kind=0xFF` with `0xFF` name bytes. **Which press wrote the byte is INFERRED, not measured** -- the byte was never read and is now gone. What is measured: an empty name draws **12** dashes, and the healthy 2026-08-18 capture already showed `0x01` plus **11** dashes, in name-content colour rather than placeholder colour. So the first name byte was already overwritten a day before the screen broke, with a value that still drew as a glyph; by 2026-08-19 it had become one the renderer treats as a line break. NAME is one DOWN from TYPE -- the field the 2026-08-18 map-derivation campaign was driving -- and nothing in the driver refuses to edit a text field. Bug #31 is the same shape (`moveCursorTo` succeeding on the wrong field, then editing whatever was there). **Not fixed: nothing stops the driver blind-editing a NAME field.** That is the prevention and it is not built. **Wrong theories, recorded so they are not re-run:** (a) *stream desync* -- no, the framing is clean; (b) *out-of-panel coordinates* -- no, every cell is in-panel, and the bounds check added for it fires only on a space the M8 legitimately draws at (0,240); (c) *the device stopped sending characters* -- an artifact of installing the raw tap AFTER the first read, which is the one carrying the post-reset repaint; (d) *a font-mode change* -- no, `font_mode` is 0 and `pitch_x/y` 8/10 on every capture, healthy and displaced alike, so this was killable offline without touching hardware. A fifth reading, that **all sixteen** instrument names wrapped, came from squinting at an INST.POOL capture still holding stale cells from the previous screen; the raw trace shows one jump, not sixteen. **Detection** (`layoutMatchRatio`, added 2026-08-18) scores how many of a screen's mapped labels are where the map says -- 23/23 on a healthy INSTRUMENT read, 2/23 on a displaced one -- surfaced by `m8_nav`, `--ui-capture`'s exit code, and `doctor`'s `decode_sane`. It had a false positive of its own, fixed here: see #33. Evidence is in `artifacts/bug32/`, which is **gitignored** and therefore local to the machine that took it: the raw taps (`b_pool_raw.bin`, `b_inst_raw.bin`, `d_inst_raw.bin`), the captures, and `make_break_page.py`, which rebuilds a before/after comparison page from them with no hardware. The method matters more than the files and is reproducible from this entry alone: SLIP-decode a `--record-raw` tap, keep the 12-byte `0xFD` draw-char commands in arrival order, map each to `(y/10)*40 + x/8`, and look for any step that is not `+1`.
| 33 | `ScreenModel.h` -- `readInstrumentType` / `getFieldMap` | FIXED | The health check that detects #32 fired on a screen with nothing wrong with it. `readInstrumentType` had no branch for an **empty** instrument slot, so it fell through to its `return "SAMPLER"` default-when-unreadable, and `layoutMatchRatio` then scored an empty slot -- which draws `TYPE NONE`, a name placeholder and essentially nothing else -- against the full 23-entry Sampler map. **Observed 2026-08-19 (fw 6.5.2, COM3)** immediately after the #32 reload: the screen was verifiably healthy (0 raster jumps in 920 draw commands, every label where it belongs) and `doctor` still reported `decode_sane: false` and `only 16% of this screen's labels are where the map says`. A detector that cries wolf on every empty instrument is a detector the next session learns to ignore, which is exactly how #32 stayed undiagnosed. **Fix:** `NONE` is recognised positively (checked last, so a real type always wins the row) and `getFieldMap(INSTRUMENT, "NONE")` returns no field map, so `layoutMatchRatio` answers -1 ("cannot judge") -- the same contract grid screens already get. The `SAMPLER` default is left intact for a genuinely unreadable TYPE row: "the slot is empty" and "the row could not be read" are different facts and were arriving as the same one. Every consumer already handled a null field array. Tests: `readInstrumentType reports an empty slot as NONE`, `layoutMatchRatio refuses to judge an empty instrument` (`[hwdecode]`).
| 34 | `Primitives.cpp` — `editValue` numeric walk | OPEN | **`set` on one field changed a DIFFERENT field.** Observed 2026-08-19 (fw 6.5.2, COM3, WavSynth instrument screen) while measuring the AMP curve. Sequence, every step read back: `LIM` stepped to `04` (**verified** `04POST`), `set AMP 00` (**verified** `00`), capture, then `set AMP FF`. Afterwards `AMP` read `FD` — not the requested `FF` — and **`LIM` read `08 POST:W3`**. No LIM command was issued between those reads, and the measurement it was taken for had to be discarded. **Suspected mechanism, NOT proven:** the pinned coarse gestures are `value_inc16 = 0x41` (EDIT+UP) and `value_dec16 = 0x21` (EDIT+DOWN), so a dropped or mistimed EDIT modifier turns a coarse step into a bare arrow — a cursor move. `AMP` sits one row above `LIM`, so a stray DOWN lands exactly on `LIM`, and the walk then continues editing whatever it is now on; coarse-stepping up from `04` clamps at `08`, which is precisely what was read. It also explains `AMP` stopping at `FD`: the loop lost its target partway. Same family as #31 and #32 — an edit landing on a field nobody asked for — but by a different route: #31 was a bad field *claim*, this is a cursor that moved *during* the walk. **Why it matters beyond one measurement:** `editValue` verifies the value it is writing but never re-verifies that the cursor is still on the field it started on, so this failure is silent by construction — and silently editing the wrong field is how #32 corrupted a project and cost two sessions. **Not fixed.** The obvious guard is to re-check `identifyCursorField` each iteration and abort if it moved, which is nearly free because the screen is re-read every iteration anyway. Not built because the mechanism is inferred rather than measured, and a guard aimed at the wrong mechanism is worse than none. First step is to reproduce it deliberately. **That step became feasible on 2026-08-24.** The reason it was hard is that the suspected mechanism is a sub-repaint event — a dropped EDIT modifier turning one coarse step into a bare cursor move — and a settle-gated read only ever sees the screen *after* the repaint finishes, by which point the cursor has moved and the walk has continued. `LiveReader` (`src/tools/m8/LiveReader.h`) samples the grid without waiting for quiet, so `editValue`'s walk can be watched iteration by iteration and the exact step where the cursor left the field identified — which either confirms the mechanism or refutes it, and the entry above is explicit that a guard aimed at the wrong one is worse than none. Note also that the *measurement* consequence is already contained: `m8_watchcapture --watch AMP --watch LIM` guards both rows every 10 ms for the whole capture window, and `hw_measure.py` renames a drifted capture. Neither fixes `editValue`; they stop a bad number entering the record. **Update 2026-08-24, two findings.** First, the guard this entry proposes is **already built** -- `Primitives.cpp` captures `startField` before the walk and re-checks `identifyCursorField` every iteration, aborting with a message naming both fields (landed in 82a176e). It had no test until now; `tests/test_device_fake.cpp` covers both halves, that it fires when the cursor drifts and that it does not fire when the cursor stays. So the *silence* is fixed even though the cause is not. Second, `m8_editwatch` swept the coarse walk on hardware -- FMSYNTH instrument screen, AMP with LIM directly below, 288 presses across nine hold/gap combinations (8-40 ms hold, 12-60 ms gap) -- with **zero cursor slips**, and AMP reached FF every run rather than stopping short at FD. Pressing harder and faster than `editValue` ever does (it carries a `readSettled(120, 200, 1200)` between presses) did not provoke it, so the mechanism is **not** simply a coarse press losing its modifier at speed. That is a narrowing, not a closure: #34 was one event in a long session and 288 presses do not prove the device never drops one. Status stays OPEN because the cause is still unknown.

---

## Related findings (confirmed real, explicitly out of driver scope)

These affect the broader M8 toolchain but are not bugs in the serial driver itself — listed here
only so they aren't lost, not tracked further in this document.

| Component | Summary |
|---|---|
| Accent-scan audit (2026-08-14) | After #24 made it three sites of the same ghost bug, every place that scans for the accent colour was audited. **All position-deciding sites are now guarded** with `ch != ' '`: `ScreenGrid::cursorRowY()` (both branches), `M8Device::cursorField()`'s X-lookup, and `findCursorCell`. Two sites intentionally left unguarded, neither of which chooses a position: `cursorMainText()`'s non-grid branch (`M8Device.cpp:127`) is scoped to an already-determined row (`pos.first != y`), so a ghost on another row cannot reach it; and `printText()` (`M8Device.cpp:386`) is a human-readable dump where a ghost only adds a spurious highlighted-row marker — adding the guard there would also strip interior spaces from the printed cursor text, which `m8_diffcheck` compares against stored goldens. |
| `m8_makeprobe` (Out-of-range parameter writes) | FIXED — Writing out-of-range values (`volume = 0xE0` > `0x7F` ceiling) caused the M8 hardware to silently treat instrument volume as `0x00` (silence). Fixed by establishing `ParamRange.h` as single source of truth for range checking. |
| Audio Analysis (`m8_spectrum` / `m8_analyze`) | FIXED — Verifying amplitude parity with saturated signals (`peak = 0.999664`) produced `1.0000` ratios by construction that could not fail. Fixed by adding `kSaturationThresh = 0.995` guard that refuses saturated comparisons (`M8_MEASUREMENT_VALIDITY_SPEC.md` V1). |
| Real M8 hardware (low notes) | EXPECTED BEHAVIOR — MIDI notes 24-27 (C-1 through D#-1) produce zero audio on real hardware due to ~3-octave down-repitching output limits. Use notes at or above C-2 / MIDI 36 for parity probes. |
| SDL3 UI clone (`src/ui/screens/project/ProjectScreenLayout.h`) | Models `SAMPLE_ROOT` as a normal PROJECT-screen field, DOWN-reachable from `SYSTEM_SETTINGS` — doesn't match real hardware (see bug #17 above). Task spawned separately to reconcile. |

---

*Cross-reference: `M8_DEVICE_CONTROL_SPEC.md` §6.5 (Tier 4.5 reliability hardening) and §7 (Tier 5
recipe attempts) carry the full hardware evidence, exact symptoms, and acceptance-test tallies
for every entry above.*
